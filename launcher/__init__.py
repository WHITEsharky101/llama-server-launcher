"""Llama.cpp Server Launcher package.

Modules:
    config    - configuration file handling, environment loading
    models    - GGUF model scanning and path resolution
    presets   - preset (RP / Coder / Commit) definitions and logic
    settings  - interactive settings editor and display
    command   - llama-server command-line builder
    server    - server process lifecycle (launch / stop)
    ui        - terminal UI helpers (headers, selection prompts)
    main      - main entry point and menu loop
"""
